// Compatibility facade: the legacy family owns the exact existing presentation.
export { resolvePresentation, surfacePalettes, presentationStyles } from "../guidance/design-families/legacy-default.js";
