## ADDED Requirements
### Requirement: The system SHALL generate a website using a backend provider
The system SHALL generate a website using a backend provider according to the implementation design.
#### Scenario: Core workflow
- **WHEN** A real OSM lead is submitted
- **THEN** the system SHALL return a schema-valid blueprint and actual generation metadata
