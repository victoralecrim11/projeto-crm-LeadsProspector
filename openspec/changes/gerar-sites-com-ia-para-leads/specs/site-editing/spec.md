## ADDED Requirements
### Requirement: The system SHALL persist edited blueprints
The system SHALL persist edited blueprints according to the implementation design.
#### Scenario: Core workflow
- **WHEN** The user edits and saves then reloads a project
- **THEN** the system SHALL restore content, visual choices, section order and metadata
